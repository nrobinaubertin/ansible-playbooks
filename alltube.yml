- hosts: "{{ host }}"
  remote_user: "{{ username }}"
  become: yes
  vars:
    packages:
      - git
      - python-docker-py
  tasks:
    - name: Install utilities for docker
      yum:
        name: "{{ packages }}"
        state: latest
    - name: Clone dockerfiles repo
      git:
        repo: "https://github.com/nrobinaubertin/dockerfiles.git"
        # we don't need to keep the repository
        dest: "/tmp/git/dockerfiles"
    - name: Create image
      docker_image:
        state: present
        name: "alltube"
        path: "/tmp/git/dockerfiles/alltube"
    - name: Start container
      docker_container:
        image: "alltube"
        name: "alltube"
        restart_policy: "unless-stopped"
        state: started
        labels:
          "traefik.backend": "alltube"
          "traefik.frontend.rule": "Host:alltube.{{ domain }}"
          "traefik.port": "8080"
          "traefik.enable": "true"
